using AutoFixture;
using Froola.Configs;

namespace Froola.Tests.Configs;

public class InitConfigConfigTests
{
    private readonly Fixture _fixture = new();

    private void SetupValidInitConfigConfigStrings(InitConfigConfig config)
    {
        // Add valid values if InitConfigConfig has string properties with restrictions
    }


    // Add more tests as needed for other required properties and parsing logic
}